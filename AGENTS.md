# 项目开发约定

本文件适用于本资料夹下的全部开发工作。

## 设计权威

- `docs/design/架空王朝起义军_游戏设计与开发交接_V0.4.md` 是当前设计基线。
- 文档内的【锁定】规则不可擅自更改；发现技术冲突时先记录并提出。
- 【原型暂定】参数应集中配置并有默认值，禁止散落硬编码。
- 【待设计】内容只允许定义稳定边界或占位接口，不自行填充玩法。

## 实现边界

- 优先完成 Vertical Slice，不提前开发多城经营、世界势力模拟、复杂装备、即时大地图、完整世界地图或 V0.5+ 内容。
- UI 不承载战斗规则；内容定义不直接引用具体页面节点。
- `Combat` 不直接修改长期存档；只返回结构化战斗结果。
- `Expedition` 负责跨战连续状态与未入库战利品；`Campaign` 只在明确结算点接收结果。
- 随机流程必须可注入和记录 Seed。
- 存档从首个可运行里程碑起包含 `save_version`，不可逆选择与永久死亡需要有回归测试。

## 质量门

- 新规则至少包含正常路径、边界值和失败路径测试。
- 所有测试必须明确报告通过数与失败数；日志中的解析错误、无效调用和未处理异常视为失败。
- UI 里程碑需要验证真实交互、结果状态和至少两种目标分辨率。
- 每个里程碑结束时更新任务台账、风险与已作出的决策。

## 变更纪律

- 一次任务只处理一个可验收目标，保留无关改动。
- 不因“以后可能需要”而增加未被当前里程碑使用的复杂抽象。
- 设计规则变更必须先更新决策记录，再修改实现与测试。
